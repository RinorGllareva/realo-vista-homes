[build]
  command = "npm run build"
  publish = "dist"

# Redirects to fix SPA routing (React Router)
[[redirects]]
  from = "/index.html"
  to = "/"
  status = 301
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
